import Fonts from "../../Utils/Fonts";

const initialValue = [
    {
      type: 'paragraph',
      children: [
        { text: 'This is editable ', font: Fonts.Arial },
      ],
    }
];

export default initialValue;